{
  "name": "polverify-circuits",
  "version": "1.0.0",
  "description": "Zero-knowledge circuits for audit verification",
  "scripts": {
    "compile": "circom audit_verification.circom --r1cs --wasm --sym --c",
    "setup": "node scripts/setup-ceremony.js",
    "test": "node scripts/test-circuit.js"
  },
  "dependencies": {
    "circomlib": "^2.0.5",
    "snarkjs": "^0.7.3",
    "ffjavascript": "^0.3.0"
  },
  "devDependencies": {
    "circom_tester": "^0.0.19"
  }
}
